import { IModes } from './../interfaces/styled';

const modes: IModes = {
	light: { backgroundColor: '#fff' },
	dim: { backgroundColor: '#192734' },
	dark: { backgroundColor: '#000' },
};

export default modes;
