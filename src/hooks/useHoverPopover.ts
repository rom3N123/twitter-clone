import React from "react";
import usePopover from "@hooks/usePopover";

const useHoverPopover = () => {
    const [state, setstate] = React.useState(false);

    const onMouseEnter = React.useCallback(() => {}, []);
};

export default useHoverPopover;
